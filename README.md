# Portfolio: SRE / Cloud Architect

Ce portfolio a été conçu avec une approche "Engineering First" :
- **Performance** : HTML/CSS/JS Pur (Vanilla). Aucun framework lourd.
- **Design** : Mode sombre, typographie technique (Inter + Roboto Mono).
- **Structure** : Contenu orienté résolution de problèmes et impact métier.

## 🚀 Déploiement sur GitHub Pages (En 2 Minutes)

Pré-requis : Un compte GitHub.

### Étape 1 : Versioning local
Ouvrez votre terminal dans ce dossier et exécutez :

```bash
git init
git add .
git commit -m "Initial commit - Portfolio V1"
```

### Étape 2 : Création du Repository
1. Allez sur [GitHub.com](https://github.com/new).
2. Créez un nouveau repository (ex: `mon-portfolio`).
3. Copiez les commandes proposées pour "push an existing repository". Elles ressemblent à ceci :

```bash
git remote add origin https://github.com/VOTRE_USER/mon-portfolio.git
git branch -M main
git push -u origin main
```

### Étape 3 : Activation de GitHub Pages
1. Une fois le code sur GitHub, allez dans l'onglet **Settings** du repository.
2. Dans le menu de gauche, cliquez sur **Pages**.
3. Sous "Build and deployment" > **Branch**, sélectionnez `main` (ou `master`) et le dossier `/ (root)`.
4. Cliquez sur **Save**.

Attendez environ 30 à 60 secondes. Une URL apparaîtra en haut de la page (ex: `https://votre-user.github.io/mon-portfolio/`).

✅ **C'est en ligne !**

## 🛠️ Personnalisation
- Modifiez `index.html` pour remplacer les textes par défaut.
- Ajustez les couleurs dans `style.css` (variables CSS au début du fichier) si besoin.
- Remplacez le lien de l'email et des réseaux sociaux.